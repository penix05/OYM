# フォームを「本当に届く」状態にする手順

サイト側の実装は完了しています。**残っているのはサーバー側の設定3ステップだけ**です。

---

## 前提：どの方式を選ぶか

| 方式 | 向いているケース | 必要なもの | 月額 |
|---|---|---|---|
| **A. 同梱のPHP（推奨）** | 一般的なレンタルサーバー（エックスサーバー、さくら、ロリポップ等）を使う | PHP 7.0以上 | 0円 |
| B. フォームSaaS | サーバーでPHPが動かない／管理画面で回答を見たい | Formspree・Tally・HubSpot等の契約 | 0〜数千円 |
| C. 現行WordPress（Contact Form 7） | すでにWordPressを運用していて、そこに寄せたい | CF7 + REST API | 0円 |

以下は **A（同梱PHP）** の手順です。B・Cの場合も、`assets/js/config.js` の `endpoint` を差し替えるだけで動きます（後述）。

---

## ステップ1：設定ファイルを作る

1. `form/config.sample.php` を **`form/config.php`** という名前でコピー
2. 中身を編集：

```php
'to'           => array('contact@oym.co.jp'),   // 通知先
'from'         => 'contact@oym.co.jp',          // 差出人（通知先と同じで問題ありません）
'token_secret' => '（ランダムな長い文字列に変更）',
```

> **`from` を入力者のアドレスにしないでください。** なりすまし扱いになり、
> Gmail等で迷惑メール判定されます。必ず `@oym.co.jp` の実在アドレスを使い、
> サーバーのDNSに **SPF**（できれば **DKIM / DMARC**）を設定してください。
> これを省くと「フォームは動いているのにメールが届かない」という事故が起きます。

通知先を増やしたい場合は配列に足すだけです：
```php
'to' => array('contact@oym.co.jp', 'eigyo@oym.co.jp'),
```

### メールの流れ（`from` も `to` も contact@oym.co.jp にした場合）

| | 通知メール | 自動返信メール |
|---|---|---|
| To | contact@oym.co.jp | 入力者 |
| From | contact@oym.co.jp | contact@oym.co.jp |
| Reply-To | **入力者** | **contact@oym.co.jp** |
| 件名 | 【サイト】お問い合わせ（教育機関の方）：山田太郎（○○専門学校） | 【On Your Mark】お問い合わせありがとうございます |

通知メールで「返信」を押せば入力者に返り、自動返信に入力者が返信すれば窓口に届きます。
どちらの方向も行き止まりになりません。件名の頭が `【サイト】` と `【On Your Mark】` で
分かれているので、受信箱でフィルタ分けもできます。

`token_secret` の作り方（どれでも可）:
```
openssl rand -base64 48
```

## ステップ2：アップロードと権限

- サイト一式をサーバーのルートへアップロード
- `form/` `docs/` 内の `.htaccess` も**必ず一緒に**アップロード
  （FTPソフトによっては隠しファイルが転送されません。表示設定を確認してください）
- `.htaccess.sample` を `.htaccess` にリネームしてルートへ設置
  （常時SSL・カスタム404・セキュリティヘッダ・キャッシュ設定が入っています）

## ステップ3：動作確認

1. `https://oym.co.jp/contact.html` を開いて実際に送信
2. `contact@oym.co.jp` に通知が届くか確認
3. 送信者側に自動返信が届くか確認
4. `https://oym.co.jp/download.html` から送信 → 完了ページのボタンでPDFが落ちるか確認
5. `https://oym.co.jp/docs/OYM_教育機関向けサービス案内.pdf` に直接アクセス →
   **403が返ればOK**（フォームを通さないと取得できない状態）

---

## スパム対策（reCAPTCHA v3）を有効にする

ハニーポット・時間トラップ・連投制限は**設定なしですでに有効**です。
それでもスパムが来る場合のみ、追加でreCAPTCHAを入れてください。

1. https://www.google.com/recaptcha/admin で v3 のキーを取得
2. `assets/js/config.js` → `recaptchaSiteKey: "サイトキー"`
3. `form/config.php` → `'recaptcha_secret' => 'シークレットキー'`
4. `contact.html` / `download.html` の下部にあるコメントアウトされた
   `<script src="https://www.google.com/recaptcha/api.js?render=...">` を有効化
   （`RECAPTCHA_SITE_KEY` を実際のサイトキーに置換）

---

## 方式B・Cを選ぶ場合

`assets/js/config.js` の `endpoint` を差し替えるだけです。

```js
// Formspree の例
endpoint: "https://formspree.io/f/xxxxxxxx",

// WordPress (Contact Form 7) の例
endpoint: "https://oym.co.jp/wp-json/contact-form-7/v1/contact-forms/123/feedback",
```

注意点：
- **Contact Form 7 の場合**、フォーム側の項目名を HTML の `name` 属性
  （`name` / `org` / `email` / `tel` / `message` / `audience` / `consent`）に揃えてください。
- **外部SaaSの場合**、資料ダウンロードのワンタイムURLは発行されません。
  `form/download.php` は使えないので、PDFの渡し方（自動返信メールに添付／限定URL）を別途決めてください。
- 外部にフォームデータを送る場合、プライバシーポリシーの
  「6．個人情報の取扱いの委託」「7．外国にある第三者への提供」に
  該当しないか確認が必要です（Formspree等は米国事業者です）。

---

## 動かないときの切り分け

| 症状 | 原因 |
|---|---|
| 「サーバー側の設定が未完了です」と出る | `form/config.php` が無い／アップロード漏れ |
| 送信はできるがメールが届かない | `from` が他社ドメイン／SPF未設定／サーバーのメール送信制限 |
| PDFが403になる | 正常。フォーム経由でのみ取得できます |
| フォーム送信後もPDFが403 | `token_secret` を書き換えた直後に古いURLを開いている（発行から1時間有効） |
| 何も起きず完了ページへ飛ぶ | `config.js` の `endpoint` が空（デモモード）になっている |
