# お問い合わせフォームのセキュリティ

「自前のPHPフォームは勧めない」というエンジニアの意見は**もっともです**。
判断できるよう、実装内容・弱点・代替案を正直に書きます。

---

## 結論を先に

- **「万全」とは言えません。** 自前のコードである以上、断言はできません
- ただし、よく知られた攻撃手法には**具体的な対策を実装し、攻撃を再現して検証済み**です
- **実務上の本当のリスクは、ハッキングよりも「メールが届かないこと」と「スパム」**です
- 御社が「自前コードを持ちたくない」と判断されるなら、**フォームサービスへの切り替えは設定1行**です

---

## 実装している対策

| 攻撃・リスク | 対策 | 検証 |
|---|---|---|
| **メールヘッダインジェクション**<br>（改行を注入して勝手にBccを追加し、迷惑メール送信の踏み台にされる） | ヘッダに入る値（お名前・学校企業名・メール）から制御文字を検出したら拒否。さらに除去した値のみをヘッダ組み立てに使用 | ✅ 実際に `Bcc:` 注入を試行し、拒否されることを確認 |
| **なりすまし送信元** | `From` は自社ドメイン固定（利用者のアドレスは `Reply-To` に入れる） | ✅ 送信ヘッダを確認 |
| **スパムボット** | ハニーポット（人に見えない入力欄）／表示から3秒以内の送信を拒否／同一IPからの連投を30秒制限 | ✅ 各挙動を確認 |
| **外部サイトからの投稿** | `Origin` / `Referer` による同一オリジン確認 | — |
| **設定ファイルの流出** | `form/config.php` を `.htaccess` で遮断。リポジトリにも含めない | ✅ |
| **資料PDFの無断取得** | `docs/` を直接アクセス禁止。HMAC署名＋1時間有効のワンタイムURLでのみ配布 | ✅ 改ざん・期限切れ・トークン無しをすべて403で拒否 |
| **問い合わせ記録の流出** | CSVを **public_html の外**（`/oym.co.jp/form-logs/`）に保存 | — |
| **入力値の異常** | 必須・形式・文字数を全項目でサーバー側検証（ブラウザ側の検証だけに頼らない） | ✅ |

### ご指摘を受けて修正した点（重要）

コードを読み直したところ、**「学校・企業名」欄から件名へ改行を注入できる経路**が残っていました。
お名前とメールアドレスには改行チェックを入れていましたが、**この欄が漏れていました**。

修正し、実際に `Bcc: attacker@example` を注入する攻撃を試して、
**拒否されること・送信メールに余計なヘッダが付かないこと**を確認しています。

ご指摘がなければ見逃していた不備です。

---

## 正直に言う弱点

### 1. reCAPTCHAが既定で無効

ハニーポットと時間制限だけでは、**本気のスパムボットは防ぎきれません**。
問い合わせが増えて迷惑メールが混ざるようなら、reCAPTCHA v3 を有効にしてください
（設定2箇所を書き換えるだけです。[フォーム設置手順.md](フォーム設置手順.md) 参照）。

### 2. 連投制限は「嫌がらせ対策」程度

IPアドレス単位の30秒制限なので、IPを変えられれば回避されます。
**大量送信を確実に止める仕組みではありません。**

### 3. 自前コードである以上、保守責任が御社に残る

PHPのバージョンアップで動かなくなる可能性はゼロではありません
（標準機能しか使っていないため低リスクですが、ゼロではない）。

### 4. 私が書いたコードは、第三者のレビューを受けていません

今回1件見つかったように、**見落としがある可能性は残ります**。
社内・外部のエンジニアにコードを見てもらえるなら、それが最も確実です。
`form/send.php` と `form/download.php` の2ファイル、合わせて300行程度です。

---

## 実は、これより大きいリスク

セキュリティより先に問題になりやすいのは、次の2つです。

### ① メールが届かない（最頻出）

`oym.co.jp` のDNSに **SPF**（できれば **DKIM / DMARC**）が設定されていないと、
**フォームは正常に動いているのに、通知がGmailの迷惑メール箱に消えます。**
問い合わせを取りこぼしても誰も気づきません。

**これはどのフォーム方式を選んでも同じです。** 最優先で設定してください。

### ② 送信できたかどうか分からない

対策として、送信内容をCSVにも記録しています。
メールが届かなくても記録は残るので、後から確認できます。

---

## 代替案（自前コードを持ちたくない場合）

**送信先は `assets/js/config.js` の1行で切り替わります。** 移行は数分で終わります。

```js
endpoint: "form/send.php",     // 現在
endpoint: "https://formspree.io/f/xxxxxxxx",   // フォームサービスに変更する場合
```

| 方式 | メリット | デメリット |
|---|---|---|
| **現在（自前PHP）** | 費用0円。データが自社サーバー内で完結 | 保守責任が自社。コードの信頼性を自分で担保する必要 |
| **フォームサービス**<br>（Formspree / Tally / HubSpot等） | 保守不要。スパム対策・到達性は事業者任せ | 月額数百〜数千円。**個人情報が外部事業者を経由** |
| **WordPress（Contact Form 7）** | 管理画面で編集できる | WordPressの保守が必要（今回それを無くす方針） |

> **フォームサービスを使う場合の注意**：学生の個人情報が海外事業者を経由することになります。
> プライバシーポリシーの「6．個人情報の取扱いの委託」「7．外国にある第三者への提供」への
> 追記が必要か確認してください。Formspree等は米国事業者です。
>
> **自前PHPには、この論点がありません。** データが自社サーバーから出ないためです。
> どちらが良いかは、保守の手間と情報の置き場所のどちらを重視するかによります。

---

## 公開前にやること

- [ ] **`oym.co.jp` にSPFを設定する**（最重要。これだけは省略不可）
- [ ] `form/config.php` の `token_secret` をランダムな長い文字列に変更
- [ ] `form/config.php` のパーミッションを `600` に（ファイル管理の「パーミッション変更」）
- [ ] `https://oym.co.jp/form/config.php` にアクセスして **403** が返ることを確認
- [ ] `https://oym.co.jp/docs/OYM_service-guide.pdf` にアクセスして **403** が返ることを確認
- [ ] 実際に送信して、通知と自動返信の両方が届くことを確認
- [ ] （任意）reCAPTCHA v3 を有効化
- [ ] （推奨）エンジニアに `form/send.php` `form/download.php` をレビューしてもらう
