# On Your Mark サイト｜公開前レビュー

静的HTML一式を確認しました。**デザイン・文章・SEOの下地・アクセシビリティの基本は、
かなりしっかり作り込まれています。**（skip-link、`aria-label`、`focus-visible`、
`prefers-reduced-motion`、canonical、OGP、構造化データ、noindex設計まで入っている）

その上で、**このまま公開すると事故になる箇所**と**改善提案**を、優先度順に整理しました。
`[済]` は今回のコミットで対応済み、`[要対応]` は御社・サーバー側の作業が必要なものです。

---

## A. 公開前に必ず対応（これが残るとサイトが機能しない）

### A-1. フォームが送信されない `[済 / 要設定]`
最大の問題でした。`oymContactInit()` が `preventDefault()` して完了ページへ遷移するだけで、
**問い合わせも資料請求も1件も届きません**。リード獲得が目的のサイトとしては致命的です。

→ 実装しました。詳細は **[フォーム設置手順.md](フォーム設置手順.md)** をご覧ください。

| 追加したもの | 内容 |
|---|---|
| `form/send.php` | メール送信・入力検証・自動返信・CSV記録 |
| `form/download.php` | 資料PDFの署名付きワンタイム配信 |
| `form/config.sample.php` | 送信先・秘密鍵の設定（`config.php` にコピーして使用） |
| `assets/js/config.js` | 送信先を1行で切り替え（PHP / Formspree / WordPress CF7） |
| `main.js` の刷新 | 非同期送信・送信中表示・二重送信防止・項目別エラー表示・失敗時のメール案内 |

**残作業（サーバー側）**：`form/config.php` の作成と、送信元ドメインの **SPF / DKIM / DMARC 設定**。
ここを省くと「フォームは動くのにメールが迷惑メール箱に消える」という一番厄介な事故になります。

### A-2. 資料PDFが誰でも直リンクで取れる `[済 / 要設定]`
`docs/OYM_教育機関向けサービス案内.pdf` に直接アクセスでき、robots.txt でも許可されていたため、
**Googleにインデックスされて「フォームを通さず資料だけ持っていかれる」状態**でした。
リード獲得の仕組みが成立しません。

→ `docs/.htaccess` で直アクセスを403に、robots.txt で `/docs/` `/form/` を Disallow、
配布は `form/download.php`（HMAC署名＋1時間の有効期限）経由に変更しました。

### A-3. 「限定公開求人」が実質公開されている `[要対応]`
`job-detail.html?id=xxx&key=xxx` の判定が **すべてブラウザ側のJavaScript** で行われており、
`assets/js/main.js` を開けば `accessKey` も求人の全データも読めます。
**学校限定の求人を実データで運用すると、企業名・給与・応募条件が誰でも閲覧可能**になります。

現状はダミーデータなので実害はありませんが、本番運用の前に必ずどちらかを選んでください。

- **(a) 当面は使わない** — `jobs.html` / `job-detail.html` / `news*.html` とダミーデータを削除し、
  求人紹介は現行の個別対応（メール・PDF）のまま。いちばん安全で、いま採れる選択肢です。
- **(b) サーバー側で認証する** — WordPress等のCMSに載せ、ログインまたはサーバー側のキー検証にする。
  クライアントサイドのキー判定は「鍵を玄関マットの下に置く」のと同じです。

### A-4. ドメイン・SSL・サーバー設定 `[要対応]`
- HTML内の canonical / OGP / sitemap.xml がすべて `https://oym.co.jp/` 前提です。
  公開ドメインが違う場合は**全ファイルを一括置換**してください。
- **常時SSL（https）**、**www あり／なしの統一**、**カスタム404が本当にHTTP 404を返すこと**が必要です。
  → ルートの `.htaccess` に一式を用意済みです（デプロイで自動設置されます）。
  （`ErrorDocument 404 /404.html` を書かないと、404ページは「200 OK」で返り、
  Googleに重複ページとして扱われます）

### A-5. ダミーデータの削除 `[要対応]`
`main.js` の `OYM_JOBS` / `OYM_NEWS` は「【サンプル求人】」「【サンプル記事】」のダミーです。
非公開ページ専用とはいえ、**JSファイルは公開されているため誰でも読めます**。
本番では実データに差し替えるか、A-3(a) を選んで削除してください。

---

## B. 公開と同時に対応したい（法務・信頼性）

### B-1. 労働者派遣事業の「マージン率等」の情報提供 `[要確認]`
労働者派遣法により、派遣元事業主には**インターネット等での情報提供が義務付け**られています
（派遣労働者数、派遣先数、マージン率、教育訓練に関する事項 等）。
現在サイト上に該当ページがありません。

実務上は**専用ページ（例：`/haken-margin.html`）を1枚作り、会社概要からリンク**する形が一般的です。
すでに別の方法（人材サービス総合サイトのみ等）で公開されている場合は、
**その公開先へのリンクを会社概要に置く**だけでも認知性が上がります。
※最終的な要否・記載内容は社労士等の専門家にご確認ください。

### B-2. 有料職業紹介事業の表示 `[要確認]`
職業安定法上、**手数料表**および**個人情報の適正管理に関する規程**の掲示・明示が求められます。
また、厚生労働省「人材サービス総合サイト」での実績情報の公開状況も併せてご確認ください。
サイト上では、少なくとも**プライバシーポリシーからの導線**か、
**会社概要「法的表記」欄への追記**が望ましいです。

### B-3. 実績数値に注記がない `[要対応]`
トップページの「内定率92%」「内定承諾率89%」「入社率98%」「紹介企業数2.1社」に、
**算出期間・対象・母数の記載がありません**。

職業安定法の誇大広告禁止、および景品表示法の観点から、
数字の下に小さく注記を入れることを強くおすすめします。

> 例）※2024年度実績。当社が就職支援を実施した専門学校○校・学生○名を対象に算出。

「約30校・80学科」も同様に、**いつ時点の数字か**を添えてください。
数字は強い訴求材料ですが、根拠が書かれていないと、
学校側の稟議・比較検討の場でかえって信頼を落とします。

### B-4. プライバシーポリシーに制定日・改定日がない `[要対応]`
内容は網羅的でよくできていますが、**「制定日」「最終改定日」が未記載**です。
ポリシーは「いつ時点のものか」が示されて初めて有効に機能します。末尾に1行追加してください。

また **10．Cookie等の利用** に「外部のアクセス解析サービスを利用する場合は別途案内します」
とある通り、**GA4等を導入したら、サービス名・オプトアウト方法の追記が必要**です。

### B-5. 個人情報の取得に「同意」の記録がなかった `[済]`
旧フォームは「プライバシーポリシーに基づき取り扱います」という**注意書きのみ**でした。
学生の学歴・応募書類といった機微な情報を扱う事業である以上、
**明示的な同意チェックボックス**があるほうが望ましいため、追加しました（必須項目）。
サーバー側でも同意なしの送信は拒否します。

### B-6. 旅行業約款の専門家確認 `[要対応]`
引き継ぎ資料にある通りです。加えて、**旅行業務取扱管理者の氏名・登録番号の掲示**、
**取引条件説明書面の交付フロー**を実務側で確認しておいてください。

---

## C. 公開直後に入れるもの（効果測定）

### C-1. アクセス解析がまったく入っていない `[要対応]`
GA4・Google Tag Manager・Search Console の**いずれも未設置**です。
このままでは「公開したが、誰がどこから来て、何件問い合わせたか分からない」状態になります。

最低限、公開日までに：
- [ ] **Google Search Console** に登録し、`sitemap.xml` を送信
- [ ] **GA4** を全ページに設置
- [ ] `thanks.html` / `dl-thanks.html` を**コンバージョンとして計測**
      （この2ページはすでに noindex かつ robots.txt で Disallow 済みなので計測に適しています）
- [ ] GA4を入れたらプライバシーポリシー10章に追記（B-4参照）

### C-2. 問い合わせの受け皿の運用 `[要対応]`
- `contact@oym.co.jp` を**誰が見るか**、**何営業日以内に返すか**を決めておく
- フォームには「通常、担当者より折り返しご連絡いたします」とありますが、
  **「2営業日以内に」など具体的な目安**があると離脱が減ります
- 自動返信メールは実装済みです（`form/config.php` の `autoreply`）

---

## D. 改善提案（優先度中〜低）

### D-1. パフォーマンス `[一部済]`
- `[済]` 全画像に `width` / `height` を付与（レイアウトのガタつき＝CLS対策）
- `[済]` ファーストビュー以外の画像を `loading="lazy"` に、メインビジュアルは `preload` + `fetchpriority="high"`
- `[要対応]` **配布PDFが4.6MB**あります。スマホで開く学校の先生も多いので、
  画像を圧縮して1〜2MB程度に落とすことをおすすめします
- `[要対応]` `ogp.png` が436KB。150KB程度まで圧縮できます
- `[要対応]` Google Fonts を4ファミリー読み込んでおり、表示開始が遅れます。
  `Caveat`（就活Cafeの手書き風文字1箇所のみ）は画像化するか、
  日本語フォントをサブセット化して自社サーバーに置くと体感速度が上がります

### D-2. JavaScript停止時に本文が消える `[済]`
`.reveal { opacity: 0 }` で全セクションを隠し、JSで表示する作りのため、
**JSが読み込めないと本文が真っ白**になります（通信エラー、社内プロキシ、古い端末で発生）。
`<noscript>` で本文を表示するフォールバックを全16ページに追加しました。

### D-3. 404ページが階層の深いURLで壊れる `[済]`
404.html が相対パス（`assets/...`）だったため、`/foo/bar` のようなURLで404になったとき
**CSS・画像・リンクがすべて壊れた状態**で表示されていました。ルート絶対パスに修正しました。

### D-4. フォームのUX `[済]`
- 入力エラーがブラウザ標準の吹き出し（英語混じり・1つずつしか出ない）でした
  → **項目の下に日本語で、全エラーを同時表示**に変更
- 送信中の表示がなく、連打で多重送信できる状態でした → **ボタンを「送信中…」にして無効化**
- 送信失敗時の案内がありませんでした → **エラー内容＋メールアドレスへの導線**を表示

### D-5. モバイルナビの操作性 `[提案]`
ハンバーガーメニューが、**Escキーで閉じない・メニュー外をタップしても閉じない・
リンク選択後も開いたまま**です。動作はしますが、改善すると体感品質が上がります。

### D-6. コンテンツ面の提案 `[提案]`
公開後に効いてくる順に：

1. **導入事例・お客様の声** — 学校名が出せなくても「都内・美容系専門学校A校」形式で、
   課題→施策→結果を3本。教育機関の稟議で最も効く材料です
2. **FAQ** — サービスページにはありますが、構造化データ（`FAQPage`）を入れると
   検索結果に直接表示される可能性があります
3. **NEWS/イベントの公開運用** — 現在クローズド設定ですが、
   更新されるコンテンツがないとSEO上も不利です。就活カフェの開催告知だけでも公開価値があります
4. **自社の採用情報** — 会社概要から「採用情報」への導線がありません
5. **SNSへの導線** — フッターにアイコンがありません（運用しているアカウントがあれば）

### D-7. 運用のしやすさ `[提案]`
ヘッダー・フッターが**16ページすべてに直書き**されています。
現状（ページ数が少ない・更新頻度が低い）では問題ありませんが、
「ナビに1項目追加」するだけで16ファイルの修正が必要です。
ページが増える見込みがあるなら、将来的にはCMS化か静的サイトジェネレータ（Astro / Eleventy）をご検討ください。

---

## 公開当日のチェックリスト

- [ ] `form/config.php` を作成し、`token_secret` を変更した
- [ ] 送信元ドメインに SPF / DKIM / DMARC を設定した
- [ ] `.htaccess` がサーバーに設置されている（デプロイで自動）
- [ ] `form/.htaccess` `docs/.htaccess` をアップロードした（隠しファイルの転送漏れに注意）
- [ ] 実ドメインが `oym.co.jp` 以外なら全ファイルを一括置換した
- [ ] https で表示され、http と www が正しくリダイレクトされる
- [ ] 存在しないURLで404ページが出て、**HTTPステータスも404**である
- [ ] お問い合わせフォームから実際に送信し、通知と自動返信の両方が届いた
- [ ] 資料ダウンロードフォームから送信し、PDFが取得できた
- [ ] PDFへの直リンクが403になる
- [ ] Search Console に登録し、sitemap.xml を送信した
- [ ] GA4 を設置し、thanks / dl-thanks をコンバージョン設定した
- [ ] 実績数値に注記を入れた（B-3）
- [ ] プライバシーポリシーに制定日を入れた（B-4）
- [ ] ダミーの求人・NEWSデータを削除、または実データに差し替えた（A-5）
