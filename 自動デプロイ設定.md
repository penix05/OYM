# GitHubへのpushでサーバーへ自動反映する設定（エックスサーバー）

設定後は **GitHubにpushするだけでサイトが更新される**状態になります。
FTPのパスワードはGitHubのSecretsに入るので、暗号化保存され、第三者からも私からも見えません。

---

## ⚠ 最初に：現行サイトの確認

引き継ぎ資料に「現行WordPress」とあります。
**`public_html` にWordPressが動いている場合、そのまま流し込むと現行サイトが壊れます。**

サーバーパネル →「ファイル管理」で `public_html/` の中を見てください。

| 中に何がある？ | 進め方 |
|---|---|
| `wp-config.php` `wp-content/` がある | **下の「安全な進め方」を読んでください** |
| 空、または古い静的ファイルのみ | そのまま `public_html/` を転送先にしてOK |

### 安全な進め方（WordPressが動いている場合）

1. まず `DEPLOY_DIR` を **`/oym.co.jp/public_html/new/`** にして公開
2. `https://oym.co.jp/new/` で全ページ・フォームを確認
3. 問題なければ、サーバーパネルの「バックアップ」で現行サイトを取得
4. `DEPLOY_DIR` を `/oym.co.jp/public_html/` に変更して再実行

ワークフローには `wp-config.php` `wp-content/` 等を除外する設定を入れてありますが、
**保険であって保証ではありません**。バックアップは必ず取ってください。

---

## ステップ1：Xserverで接続情報を確認する

### FTPホスト名・ユーザー名
サーバーパネル → **「FTPアカウント設定」** → ドメインを選択 → **「FTPソフト設定」** タブ

- **FTPホスト名**：`svXXXX.xserver.jp` のような形式
- **ユーザー名**：サーバーIDまたは追加したFTPアカウント名
- パスワードは表示されません。不明なら同じ画面で再設定できます

### アップロード先のパス
サーバーパネル → **「ファイル管理」** を開くと構造が見えます。

```
/home/（サーバーID）/
  └ oym.co.jp/
      ├ public_html/     ← Web公開領域（https://oym.co.jp/ に対応）
      ├ log/
      └ mail/
```

FTPログイン直後の位置から数えて **`/oym.co.jp/public_html/`** が転送先です。

### 暗号化接続（FTPS）
同じ「FTPソフト設定」画面に暗号化接続の案内があります。
**平文FTPはパスワードがそのまま流れる**ので、FTPSが使えるならそちらにしてください。
ワークフローの既定値は `ftps` です。使えない場合のみ `ftp` に変更します。

---

## ステップ2：GitHubに登録する

リポジトリの **Settings → Secrets and variables → Actions**

### 「Secrets」タブ（暗号化・閲覧不可）

| 名前 | 値 |
|---|---|
| `FTP_SERVER` | `svXXXX.xserver.jp` |
| `FTP_USERNAME` | FTPユーザー名 |
| `FTP_PASSWORD` | FTPパスワード |

### 「Variables」タブ（暗号化なし・後から変更しやすい）

| 名前 | 値 |
|---|---|
| `DEPLOY_DIR` | `/oym.co.jp/public_html/`（**末尾のスラッシュ必須**） |
| `FTP_PROTOCOL` | `ftps`（FTPSが使えない場合のみ `ftp`） |

> パスだけVariablesにしているのは、`new/` でのテストから本番へ切り替えるときに
> 値の書き換えだけで済ませるためです。

---

## ステップ3：まず「確認のみ」で実行する

**いきなり本番転送しないでください。**

1. リポジトリの **Actions** タブ → 左の **「Deploy to Xserver」**
2. **「Run workflow」** → `dry_run` に **チェックを入れたまま** 実行
3. ログに「どのファイルが転送されるか」が出るので確認
4. 問題なければ、もう一度 Run workflow → `dry_run` の**チェックを外して**実行

以降は、このブランチにpushするたびに自動でデプロイされます。

---

## 転送されないもの（意図的な除外）

| 除外対象 | 理由 |
|---|---|
| `form/config.php` | サーバー側だけに置く実設定。**上書き・削除されません** |
| `form/logs/` | 問い合わせのCSV記録。消えると困るため |
| `*.md` | 作業用ドキュメント。公開不要 |
| `制作会社への引き継ぎ.txt` | 社内資料。公開すると社外に見えてしまいます |
| `.htaccess.sample` | 手動で `.htaccess` にリネームして設置するもの |
| `.github/` `.git*` | リポジトリの管理ファイル |
| `wp-config.php` `wp-content/` 等 | 現行WordPressを巻き込まないための保険 |

`form/.htaccess` と `docs/.htaccess` は**転送されます**（PDFの直リンクを防ぐために必要）。

---

## 初回だけ手動で必要な作業

自動デプロイの対象外なので、FTPソフトかファイル管理から1回だけ行ってください。

1. **`form/config.php` の作成** — `form/config.sample.php` を参考に（[フォーム設置手順.md](フォーム設置手順.md)）
2. **`.htaccess` の設置** — `.htaccess.sample` の内容をルートに `.htaccess` として配置
   ※ WordPressの `.htaccess` が既にある場合は、**追記**であって置き換えではありません。既存の記述を消さないでください

---

## うまくいかないとき

| 症状 | 原因 |
|---|---|
| `DEPLOY_DIR が未設定です` | Variables に登録されていない／名前のスペルミス |
| `末尾にスラッシュが必要です` | `/oym.co.jp/public_html` → `/oym.co.jp/public_html/` |
| 認証エラー | ユーザー名がメールアドレス形式になっていないか確認 |
| FTPSで接続できない | `FTP_PROTOCOL` を `ftp` に変更（暗号化されない点は許容する） |
| 転送されるが反映されない | `DEPLOY_DIR` が `public_html` の外を指している |
| 毎回すべてのファイルが転送される | サーバー上の `.ftp-deploy-sync-state.json` が消えている。動作に支障はありません |
