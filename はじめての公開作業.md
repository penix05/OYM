# はじめての公開作業（クリック手順）

専門用語をできるだけ使わずに、画面ごとに書きます。
**作業は「GitHubの画面」と「エックスサーバーの画面」の2つだけ**です。

---

## まず、言葉の説明

| 言葉 | 意味 |
|---|---|
| **デプロイ** | GitHubにあるファイルをサーバーに送ること。要するに「サイトを更新する」こと |
| **旧ファイル** | いまサーバーに入っている、古いサイトのファイル（下の図の「消す」印のもの） |
| **リポジトリ** | GitHub上の、サイトのファイル置き場。`penix05/OYM` のこと |
| **Secrets（シークレット）** | GitHubに預ける「人に見せない値」。パスワードなど |
| **Variables（バリアブル）** | GitHubに預ける「見えてもいい値」。転送先のフォルダ名など |

## いまサーバーに入っているもの

先日いただいた画面（`oym.co.jp` → `public_html`）の中身です。

```
public_html/
  ├ css/        ← 古いサイト    【あとで消す】
  ├ images/     ← 古いサイト    【あとで消す】
  ├ js/         ← 古いサイト    【あとで消す】
  ├ scss/       ← 古いサイト    【あとで消す】
  ├ wp/         ← WordPress     【あとで消す】
  ├ index.php   ← 古いトップページ   【あとで消す】
  ├ .htaccess   ← 設定ファイル   【自動で新しくなる】
  └ .user.ini   ← PHPの設定     【そのまま残す】
```

## 作業が終わったあとの姿

```
public_html/
  ├ index.html          ← 新しいトップページ
  ├ company.html ほか   ← 各ページ
  ├ assets/             ← 画像・デザイン・プログラム
  ├ form/               ← お問い合わせフォームの処理
  ├ docs/               ← 配布PDF
  ├ .htaccess           ← 新しい設定
  └ .user.ini           ← そのまま
```

---

# 【第1部】GitHubの画面での作業

## 手順1：5つの値を登録する

### 1-1. 画面を開く

ブラウザで **https://github.com/penix05/OYM** を開く

→ 上のほうにある **「Settings」**（歯車マーク）をクリック
→ 左側のメニューを下にスクロールして **「Secrets and variables」** をクリック
→ その下に出てくる **「Actions」** をクリック

### 1-2. 「Secrets」タブに3つ登録

画面に **「Secrets」** と **「Variables」** という2つのタブがあります。
まず **「Secrets」** タブで、緑色の **「New repository secret」** ボタンを押します。

**1つめ**
- Name の欄に： `FTP_SERVER`
- Secret の欄に： `sv8416.xserver.jp`
- **「Add secret」** を押す

**2つめ**（また「New repository secret」を押す）
- Name の欄に： `FTP_USERNAME`
- Secret の欄に： エックスサーバーのFTPユーザー名
- **「Add secret」** を押す

**3つめ**
- Name の欄に： `FTP_PASSWORD`
- Secret の欄に： そのパスワード
- **「Add secret」** を押す

> Name は**大文字・アンダーバーまで、そのとおりに**入力してください。
> 1文字でも違うと動きません。

### 1-3. 「Variables」タブに2つ登録

**「Variables」** タブに切り替えて、**「New repository variable」** を押します。

**1つめ**
- Name： `DEPLOY_DIR`
- Value： `/oym.co.jp/public_html/`  ← **最後のスラッシュまで入れる**

**2つめ**
- Name： `FTP_PROTOCOL`
- Value： `ftps`

---

## 手順2：まず「お試し」で動かす

**この操作ではファイルは1つも送られません。** 接続できるかだけ確認します。

1. **https://github.com/penix05/OYM** に戻る
2. 上のメニューの **「Actions」** タブをクリック
3. 左側の一覧から **「Deploy to Xserver」** をクリック
4. 右のほうにある **「Run workflow」** という灰色のボタンをクリック
5. 小さな窓が出ます。**チェックボックスはそのまま**（チェックが入った状態）
6. 窓の中の緑の **「Run workflow」** を押す

しばらくすると一覧に新しい行が現れます。クリックすると進行状況が見られます。

- **緑のチェック ✅** → 成功。手順3へ
- **赤いバツ ❌** → 失敗。その画面をそのまま私に貼ってください

---

## 手順3：本番に送る

手順2とまったく同じ操作を、**チェックボックスだけ外して**行います。

1. **「Actions」** タブ → **「Deploy to Xserver」** → **「Run workflow」**
2. 出てきた窓の**チェックボックスのチェックを外す**
3. 緑の **「Run workflow」** を押す

緑のチェックが付けば、**サーバーに新しいサイトのファイルが置かれました。**

> **この時点では、まだトップページは古いままかもしれません。** 正常です。
> 古い `index.php` がまだ残っているためで、手順6で消します。

---

# 【第2部】エックスサーバーの画面での作業

## 手順4：フォームの設定ファイルを作る

これだけは自動でできません（パスワードに相当する値を含むためです）。

1. サーバーパネル → **「ファイル管理」**
2. `oym.co.jp` → `public_html` → **`form`** フォルダを開く
3. `config.sample.php` があるはずです。これを選んで **「コピー」**
4. コピーされたファイルの名前を **`config.php`** に変更する
5. `config.php` を選んで **「編集」** をクリック
6. 下のほうにある、この行を探す

```php
'token_secret'  => 'CHANGE-ME-TO-A-LONG-RANDOM-STRING',
```

7. `CHANGE-ME-TO-A-LONG-RANDOM-STRING` の部分だけを、
   **キーボードをでたらめに40文字以上叩いた文字列**に置き換える

```php
'token_secret'  => 'k3j9xQ2mZp7vB4nL8wR5tY6uI1oA0sD3fG7hJ2kL9xC4vB',
```

8. **保存**

> この文字列は誰かに教えるものではありません。忘れても大丈夫です。
> 資料PDFのダウンロードURLを偽造されないための鍵です。

---

## 手順5：動いているか確認する

ブラウザで、次のURLを順番に開いてください。

| URL | 期待される結果 |
|---|---|
| `https://oym.co.jp/company.html` | 新しい会社概要ページが表示される |
| `https://oym.co.jp/contact.html` | 新しいお問い合わせページが表示される |

`contact.html` で実際にフォームを送信してみてください。

- `contact@oym.co.jp` に通知メールが届く
- 入力したメールアドレスに自動返信が届く

> **うまくいかないときは、その画面やエラーを私に貼ってください。**

---

## 手順6：古いファイルを消す

**ここでトップページが新しくなります。**

サーバーパネル → **「ファイル管理」** → `oym.co.jp` → `public_html`

次のものを選んで削除します。

```
✅ 消す：  css   images   js   scss   wp   index.php
❌ 残す：  .user.ini
❌ 残す：  .htaccess          （手順3で新しくなっています）
❌ 残す：  assets  form  docs  および .html ファイル（新しいサイト）
```

> **`index.php` を消すと、トップページが新しいサイトに変わります。**
>
> 不安であれば、削除ではなく**リネーム**でも構いません。
> `index.php` → `index.php.old` にすれば、元に戻したくなったとき名前を戻すだけです。

---

## 手順7：キャッシュを消す

サーバーパネルには、表示を速くするために**ページを一時保存する機能**が有効になっています。
これを消さないと、**変更したのに古い画面が出続けます。**

サーバーパネル → **「サーバーキャッシュ設定」** → `oym.co.jp` → **キャッシュを削除**

その後、`https://oym.co.jp/` を開いて新しいサイトになっていれば完了です。

> それでも古い画面が出る場合は、ブラウザ側のキャッシュです。
> **Ctrl + F5**（Windows）で強制的に再読み込みしてください。

---

# 完了後：今後の更新について

**もう作業は不要です。**

こちらでファイルを直してGitHubに送ると、**自動でサーバーに反映されます。**
「この文章を変えて」と言っていただければ、それだけで更新されます。

---

# 困ったときは

| 状況 | どうするか |
|---|---|
| GitHubで赤いバツが出た | その画面を私に貼ってください |
| ページが崩れている | そのページのURLと見た目を貼ってください |
| フォームを送ってもメールが来ない | まず迷惑メールフォルダを確認。無ければ私に連絡を |
| 変更したのに画面が変わらない | 手順7のキャッシュ削除 → それでもダメなら Ctrl+F5 |
| 元に戻したい | `index.php.old` を `index.php` に戻せば旧サイトに戻ります |

**分からない画面が出たら、スクリーンショットを貼ってください。** そこから案内します。
