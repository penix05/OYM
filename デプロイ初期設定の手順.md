# FTPサブアカウント作成 → GitHub登録 → 初回デプロイ

所要時間：15分程度。**この作業でサイトの表示は変わりません**（最後にdry-runで止めるため）。

---

## パートA：エックスサーバーでFTPサブアカウントを作る

### なぜサブアカウントなのか

マスターFTPアカウント（ユーザー名＝サーバーID）のパスワードは、
**サーバーパネルへのログインパスワードと同じ**です。これをGitHubに登録すると、
万一漏れたときにサーバー全体・他ドメイン（karehasu.co.jp 等）・メール設定まで操作されます。

デプロイ専用のサブアカウントなら、
**公開ディレクトリだけに限定でき、不要になったら削除するだけ**で済みます。

### 手順

**1. サーバーパネルにログイン**

Xserverアカウント（会員ページ）→ 該当サーバーの **「サーバー管理」** ボタン

**2. 「FTP」欄の「サブFTPアカウント設定」を開く**

**3. ドメイン `oym.co.jp` を選択**

**4. 「FTPアカウント追加」タブを開く**

**5. 次のように入力**

| 項目 | 入力する値 |
|---|---|
| FTPユーザーID | `deploy`（分かりやすい名前で構いません） |
| パスワード | **強いランダムな文字列**（下記参照） |
| アクセス可能ディレクトリ | **`oym.co.jp/public_html`** ← ここが重要 |
| 容量制限 | 無制限のままでOK |

> **アクセス可能ディレクトリは必ず `public_html` に絞ってください。**
> 空欄やドメイン直下にすると、`mail/` `log/` など公開領域外まで触れてしまいます。

**パスワードの作り方**：パスワード管理ツールの生成機能を使うのが確実です。
無ければ、大小英字・数字・記号を混ぜた20文字以上にしてください。

> ⚠ **パスワードはこのチャットに貼らないでください。** 会話ログに平文で残ります。
> GitHubのSecretsに直接入力してください。

**6. 「確認画面へ進む」→「追加する」**

**7. 作成後に表示されたユーザー名を、表示されたとおりに控える**

エックスサーバーでは `deploy@oym.co.jp` のような形式になる場合があります。
その場合は **`@` 以降も含めて全部が正しいユーザー名**です。

### FTPホスト名も控える

**「FTPアカウント設定」→ ドメイン選択 →「FTPソフト設定」タブ**

`svXXXX.xserver.jp` のような文字列が **FTPホスト名** です。

---

## パートB：GitHubに登録する

### 1. 設定画面を開く

`https://github.com/penix05/OYM` → 上部の **Settings** タブ
→ 左メニュー **Secrets and variables** → **Actions**

> Settings タブが見えない場合、そのリポジトリの管理権限がありません。
> オーナーアカウントでログインし直してください。

### 2. 「Secrets」タブに3つ登録

**New repository secret** を押して、1つずつ追加します。

| Name（そのままコピー） | Secret |
|---|---|
| `FTP_SERVER` | `svXXXX.xserver.jp` |
| `FTP_USERNAME` | `deploy@oym.co.jp`（表示されたとおりに） |
| `FTP_PASSWORD` | 設定したパスワード |

> Secretsは登録後、**本人にも再表示されません**（上書きのみ可能）。
> パスワードは手元にも保管しておいてください。

### 3. 「Variables」タブに2つ登録

**New repository variable** を押して追加します。

| Name | Value |
|---|---|
| `DEPLOY_DIR` | `/oym.co.jp/public_html/` ← **末尾のスラッシュ必須** |
| `FTP_PROTOCOL` | `ftps`（接続できなければ `ftp` に変更） |

> `DEPLOY_DIR` をVariables側に置いているのは、後から値を書き換えるだけで
> 転送先を変えられるようにするためです。

---

## パートC：まず「確認のみ」で実行する

**いきなり本番転送しないでください。**

1. リポジトリの **Actions** タブ
2. 左の **「Deploy to Xserver」** をクリック
3. 右の **「Run workflow」** ボタン
4. **`dry_run` にチェックが入ったまま**「Run workflow」を実行
5. 実行結果を開き、ログで転送対象ファイルを確認

この時点では**1ファイルも転送されません**。設定が正しいかだけを確認できます。

### 確認するポイント

- 認証エラーが出ていないか
- 転送先が `/oym.co.jp/public_html/` になっているか
- `wp/` `css/` `images/` `js/` `scss/` `index.php` が対象に入っていないこと
  （入っていたら止めてください。除外設定が効いていません）

問題なければ、もう一度 Run workflow →
**`dry_run` のチェックを外して**実行すると、実際に転送されます。

---

## ⚠ 登録後は、pushするたびに自動デプロイされます

ワークフローは、このブランチへのpushでも起動します。
**移行作業中は手動実行だけにしたい**場合は、`.github/workflows/deploy.yml` の
先頭付近にある次のブロックを削除（またはコメントアウト）してください。

```yaml
  push:
    branches:
      - main
      - claude/site-launch-improvements-58duhc
```

残すと「pushすれば反映される」状態、消すと「Actionsから手動実行したときだけ反映」になります。
落ち着くまでは手動のみにしておくのが無難です。

---

## エラーが出たときは

| メッセージ | 原因と対処 |
|---|---|
| `FTP_SERVER が未設定です` 等 | Secrets / Variables の名前のスペルミス。大文字小文字も一致させる |
| `DEPLOY_DIR は末尾にスラッシュが必要です` | `/oym.co.jp/public_html` → `/oym.co.jp/public_html/` |
| 認証エラー（530など） | ユーザー名を `@oym.co.jp` まで含めて正確に。パスワードの前後に空白が入っていないか |
| FTPSで接続できない | `FTP_PROTOCOL` を `ftp` に変更（暗号化されない点は許容する） |
| タイムアウト | `FTP_PROTOCOL` を `ftp` にするか、時間をおいて再実行 |

エラーログをそのまま共有いただければ、こちらで原因を特定します。
**ただしログにパスワードが含まれていないか確認してから貼ってください。**

---

## この後の流れ

初回デプロイが通ったら、[WordPressからの移行.md](WordPressからの移行.md) のステップ3以降
（`form/config.php` の設置 → フォーム検証 → `.htaccess` 差し替え）へ進みます。
