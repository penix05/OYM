# 手動アップロードでの公開手順（GitHub Actionsを使わない方法）

## 用意するもの

- **`oym-site-upload.zip`** … アップロードする一式（別途お渡ししています）
- **`htaccess-新サイト用.txt`** … 最後に設置する `.htaccess` の中身
- エックスサーバーの**ファイル管理**、または FTPソフト（FileZilla / WinSCP など）

### FTPアカウントについて

**手動アップロードなら、いつもお使いのアカウントで問題ありません。**

先にサブアカウントをおすすめしたのは、**パスワードをGitHubに預ける場合**の話でした。
手動アップロードではパスワードが御社のPCの外に出ないため、その心配は当てはまりません。

| 項目 | 値 |
|---|---|
| FTPホスト名 | `sv8416.xserver.jp` |
| ユーザー名 | サーバーID |
| パスワード | サーバーパネルのログインパスワード |
| 転送先 | `/oym.co.jp/public_html/` |

---

## ★ 作業前に必ず：バックアップ

サーバーパネル →「バックアップ」で、**ファイルとデータベース（MySQL）の両方**を取得し、
**社内PCなどサーバー外に保管**してください。

記事も問い合わせ履歴もデータベースの中にあり、ファイルだけでは復元できません。

---

## ステップ1：ZIPを展開する

`oym-site-upload.zip` を解凍すると、`oym-site` というフォルダができます。

```
oym-site/
  ├ index.html
  ├ company.html   …ほか各ページ
  ├ assets/
  ├ docs/
  └ form/
```

> **⚠ アップロードするのは `oym-site` フォルダの「中身」です。**
> `oym-site` フォルダごと上げると `https://oym.co.jp/oym-site/...` になってしまいます。

---

## ステップ2：アップロードする

**現行サイトのファイルとは名前が1つも重ならないので、この時点では現行サイトは動いたままです。**
（`css/` `images/` `js/` `scss/` `wp/` と、新サイトの `assets/` `form/` `docs/` は別物）

### 方法A：ファイル管理を使う（おすすめ）

隠しファイルの転送漏れが起きないので確実です。

1. サーバーパネル →「ファイル管理」
2. `oym.co.jp` → `public_html` へ移動
3. **ZIPのまま**「アップロード」
4. アップロードしたZIPを選択して「**解凍**」
5. 展開された `oym-site` フォルダの中身を `public_html` 直下へ移動
6. 空になった `oym-site` フォルダとZIPを削除

### 方法B：FTPソフトを使う

1. `sv8416.xserver.jp` に接続
2. **隠しファイルを表示する設定にする** ← ★重要
   - FileZilla：メニュー「サーバー」→「強制的に隠しファイルを表示する」
   - WinSCP：環境設定 →「パネル」→「隠しファイルを表示する」
3. 解凍した `oym-site` の**中身をすべて**選択して `/oym.co.jp/public_html/` へ転送
4. 転送モードは「**自動**」または「**バイナリ**」

> **隠しファイルの転送漏れが、この作業で最も多い失敗です。**
> 新サイトには `form/.htaccess` `docs/.htaccess` `form/logs/.htaccess` の3つがあり、
> これらが無いと**PDFが誰でも直リンクで取れてしまいます**。
>
> 転送後、サーバー上に3つとも存在するか必ず確認してください。

---

## ステップ3：`form/config.php` を作る

フォームの送信先設定です。これが無いとフォームは動きません。

1. サーバー上の `form/config.sample.php` をコピーして **`form/config.php`** を作る
   （ファイル管理なら「コピー」→ 名前変更、FTPソフトならローカルで作ってアップロード）
2. 「編集」で開いて、次の2箇所を書き換える

```php
'from'         => 'contact@oym.co.jp',   // 既定値のままでOK
'to'           => array('contact@oym.co.jp'),  // 既定値のままでOK

'token_secret' => 'ここをランダムな長い文字列に変更',   // ★必ず変更
```

`token_secret` は、資料PDFのダウンロードURLを偽造されないための鍵です。
**キーボードをでたらめに40文字以上叩いた文字列**で構いません。
外部に教える必要はなく、忘れても再設定できます（発行済みのURLが無効になるだけです）。

> 変更しないままだと、フォームを通さずにPDFのダウンロードURLを作られる可能性があります。

---

## ステップ4：動作を確認する（この時点でトップ以外は新サイトです）

ブラウザで直接開いて確認してください。

```
https://oym.co.jp/company.html
https://oym.co.jp/service-school.html
https://oym.co.jp/contact.html
https://oym.co.jp/download.html
```

- [ ] ページが正しく表示される（デザインが崩れていない）
- [ ] `contact.html` から実際に送信 → `contact@oym.co.jp` に届く
- [ ] 送信者側に自動返信が届く
- [ ] `download.html` から送信 → PDFがダウンロードできる
- [ ] `https://oym.co.jp/docs/OYM_service-guide.pdf` に直接アクセスして **403** が出る
      （200でPDFが開いたら、`docs/.htaccess` の転送漏れです）

> **表示が変わらない場合はキャッシュです。**
> サーバーパネル →「サーバーキャッシュ設定」→ キャッシュ削除を試してください。

---

## ステップ5：トップページを切り替える

**この順番で行ってください。逆にすると一時的にサイトが壊れます。**

### 5-1. `.htaccess` を差し替える

1. 現行の `public_html/.htaccess` を**ダウンロードして手元に保存**（戻せるように）
2. `htaccess-新サイト用.txt` の中身で、サーバー上の `.htaccess` を書き換える
   - ファイル管理なら「編集」で全選択→貼り付け
   - FTPソフトなら、ローカルで `.htaccess`（拡張子なし）として保存して上書き転送

### 5-2. `index.php` を削除する

`public_html/index.php` を削除します。これでトップが `index.html`（新サイト）になります。

> `index.php` を先に消すと、WordPressの書き換え規則だけが残り、
> 存在しないURLへのアクセスが全部エラーになります。**必ず `.htaccess` を先に。**

### 5-3. キャッシュを削除する

サーバーパネル →「サーバーキャッシュ設定」→ キャッシュ削除

### 5-4. 確認

- [ ] `https://oym.co.jp/` が新サイトになっている
- [ ] 存在しないURL（例 `https://oym.co.jp/zzz`）で404ページが出る
- [ ] `http://oym.co.jp/` が `https://` に転送される

---

## ステップ6：旧ファイルを削除する（数日おいてから）

問題が出ないことを確認してから、`public_html/` の以下を削除します。

```
wp/        ← WordPress本体
css/
images/
js/
scss/
```

**`.user.ini` は残してください。** XserverのPHP設定で、消すとPHPの挙動が変わる可能性があります。

> **現在の問い合わせフォームがWordPress側で動いている場合、`wp/` を消すと止まります。**
> 新フォームで問い合わせが届くことを確認してから削除してください。
>
> 急ぐ必要はありませんが、`wp/` を残すなら**WordPressの更新は続けてください**。
> 放置されたWordPressは改ざん被害の定番の入口です。使わないなら消すのが最も安全です。

---

## ステップ7：計測を立ち上げる

検索流入がほぼない状態からの再スタートなので、**ここが今回いちばん効きます。**

- Google Search Console に登録し、`sitemap.xml` を送信
- GA4 を全ページに設置
- `thanks.html` / `dl-thanks.html` をコンバージョンとして設定
- GA4を入れたら、プライバシーポリシー10章に利用しているサービス名を追記

---

## 今後の更新のしかた

HTMLを直接編集して、該当ファイルだけをFTPで上書きすれば反映されます。
文言の修正なら、そのページのHTMLファイル1つを差し替えるだけです。

更新のたびに**サーバーキャッシュの削除**が必要になる場合があります。

> GitHubへのpushで自動反映する仕組み（`.github/workflows/deploy.yml`）も用意してあります。
> 現在は誤作動しないよう無効化してあるので、必要になったら
> [デプロイ初期設定の手順.md](デプロイ初期設定の手順.md) を参照してください。

---

## うまくいかないときは

| 症状 | 原因 |
|---|---|
| ページが真っ白／デザインが崩れる | `assets/` の転送漏れ。フォルダごと再転送 |
| `https://oym.co.jp/oym-site/` になってしまった | フォルダごと上げている。中身だけを移動 |
| PDFが403にならない（誰でも取れる） | `docs/.htaccess` の転送漏れ（隠しファイル） |
| `form/` が500エラー | `form/.htaccess` が壊れている。再転送 |
| 「サーバー側の設定が未完了です」 | `form/config.php` が無い（ステップ3） |
| フォーム送信はできるがメールが届かない | `oym.co.jp` のSPF設定。サーバーパネルで確認 |
| 変更したのに表示が変わらない | サーバーキャッシュ。パネルから削除 |
