# 現行サイト → 新サイト（静的HTML）への切り替え手順

**方針：現行サイトを全面的に作り替える。検索流入がほぼないため、旧URLの引き継ぎは最小限。**

---

## 現在のサーバー構成（2026年9月時点・実機確認済み）

```
/oym.co.jp/public_html/
  ├ css/        11/19 2024   現行サイトのCSS
  ├ images/     11/25 2024   現行サイトの画像
  ├ js/         11/11 2024   現行サイトのJS
  ├ scss/       11/19 2024   CSSのビルド元（Sass）
  ├ wp/          9/10 11:23  ★WordPress本体（wp-admin / wp-content / wp-includes）
  ├ .htaccess    1/10 2025   ★おそらくWordPressの書き換えルール
  ├ .user.ini    6/4 2024    XserverのPHP設定（★触らない）
  └ index.php   10/23 2024   ★おそらく wp/ を呼び出している
```

WordPressが `wp/` 配下にまとまっているため、**削除の影響範囲が明確で安全**です。

### 新サイトとファイル名が1つも衝突しません

新サイトは `assets/` `form/` `docs/` 配下にまとまっており、
`css/` `images/` `js/` `scss/` `wp/` とは別名です。root直下のファイルも `index.html` と
`index.php` で別物です。

**つまり、現行サイトを動かしたまま新サイトを同じ場所に置けます。**
この性質を使って、切り替えのリスクを最小化します。

---

## ⚠ 作業前に確認すること

### 1. `index.php` と `.htaccess`（確認済み）

両ファイルの中身を確認しました。**想定どおりの構成で、特殊な作り込みはありません。**

**`index.php`** — WordPress標準のフロントコントローラーです。

```php
require __DIR__ . '/wp/wp-blog-header.php';
```

WordPress本体を `wp/` に置き、ルートから配信する標準構成です。
**新サイトでは不要なので削除します。**

**`.htaccess`** — 3つのブロックで構成されています。

| 内容 | 新サイトでの扱い |
|---|---|
| 常時SSL（http → https） | **そのまま引き継ぎ** |
| `Ngx_Cache_*`（エックスサーバーのキャッシュ設定） | **そのまま引き継ぎ**（下記★参照） |
| `BEGIN WordPress` 〜 `END WordPress` | **削除** |

WordPressブロックの中身は標準のもので、
「実ファイルもディレクトリも存在しないURL」だけを `/index.php` に回す作りです。

```apache
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
```

**これが、新サイトを並存させられる理由です。** `company.html` などは実ファイルとして
存在するため、この規則に引っかからずそのまま配信されます。
WordPressに回るのはトップページ（`/`）だけです。

→ 差し替え後の `.htaccess` はリポジトリのルートに用意済みで、**デプロイで自動的に設置されます**。
　 引き継ぎ部分はそのままコピーしてあります。

念のため、作業前に現行の2ファイルを**ダウンロードして手元に保存**しておいてください。

### ★ サーバーキャッシュが有効になっています（重要）

`.htaccess` に次の2行が入っていました。

```apache
SetEnvIf Request_URI ".*" Ngx_Cache_NoCacheMode=off
SetEnvIf Request_URI ".*" Ngx_Cache_AllCacheMode
```

これはエックスサーバーの**サーバーキャッシュ設定**（サーバーパネルが自動生成する行）で、
**全URLがキャッシュ対象**になっています。

**デプロイしても表示が変わらない場合、キャッシュが原因です。**
サーバーパネル →「サーバーキャッシュ設定」→ 対象ドメインの**キャッシュを削除**してください。
切り替え作業中は毎回これが必要になります。

- この2行はパネルが管理しているため、手で書き換えないでください
- パネルで設定を変更すると `.htaccess` が上書きされることがあります。
  その場合は、もう一度デプロイを実行すればリポジトリの内容に戻ります
- フォームの応答がキャッシュされないよう、`form/.htaccess` で
  `Cache-Control: no-store` を指定してあります

### 2. 現在のお問い合わせフォームがどこで動いているか ★重要

現行サイトのフォームがWordPress側（Contact Form 7など）で動いている場合、
**`wp/` を消した瞬間に現行の問い合わせ窓口が止まります。**

→ 新フォームの動作確認が終わるまで `wp/` は消さないでください（手順に組み込んであります）。
→ 過去の問い合わせがDBに保存されている場合、バックアップはデータベースが必須です。

### 3. 新サイトに無いコンテンツがないか

- ブログ記事・お知らせに、持っていきたい文章はないか
- 実績紹介・採用情報など、新サイトに存在しないページはないか
- **名刺・パンフレット・求人媒体・SNSに旧URLを記載していないか**
  （検索流入がなくても、ここからの直接アクセスは生きています）

### 4. `wp/` が9月10日に更新されている

WordPressの自動更新が動いている可能性が高いです（＝まだ生きている）。
念のため、WordPress管理画面に入って、意図しないプラグインやユーザーが増えていないか
確認しておくと安心です。

---

## 手順

### ステップ1：バックアップ

サーバーパネル →「バックアップ」

**★ ファイルとデータベース（MySQL）の両方**を取得してください。
記事も問い合わせ履歴もDBの中にあり、ファイルだけでは復元できません。

ダウンロードしたバックアップは**サーバー外（社内PC・クラウド）に保管**してください。

### ステップ2：新サイトをデプロイ（現行サイトは動いたまま）

GitHubの Variables で `DEPLOY_DIR` = **`/oym.co.jp/public_html/`**

ファイル名が衝突しないので、現行サイトのファイルは1つも上書きされません。

> **⚠ トップページがこの時点で切り替わる可能性があります。**
> Xserverの `DirectoryIndex` が `index.html` を `index.php` より優先していると、
> `index.html` が置かれた瞬間にトップが新サイトになります。
> **アクセスの少ない時間帯に作業**し、次のステップの準備（`form/config.php` の中身）を
> 済ませてから実行してください。
>
> 逆に、キャッシュが効いているため**しばらく旧トップが表示され続ける**こともあります。
> どちらに転んでも慌てないよう、切り替わりの確認は
> サーバーパネルでキャッシュを削除してから行ってください。

### ステップ3：`form/config.php` を設置してフォームを検証

[フォーム設置手順.md](フォーム設置手順.md) のとおり、`form/config.sample.php` を元に
`form/config.php` を作成します。**内容を事前に用意しておくと、切り替え時間が短くなります。**

検証：
- `https://oym.co.jp/contact.html` から実際に送信 → `contact@oym.co.jp` に届くか
- 自動返信が届くか
- `https://oym.co.jp/download.html` から送信 → PDFが取得できるか

### ステップ4：各ページを直接URLで検証

トップ以外は、`.htaccess` を触らなくても実ファイルが直接返るため、この時点で確認できます。

```
https://oym.co.jp/company.html
https://oym.co.jp/service-school.html
https://oym.co.jp/service-business.html
https://oym.co.jp/service-students.html
https://oym.co.jp/privacy.html
https://oym.co.jp/travel-terms.html
```

### ステップ5：切り替え（この順番で行ってください）

1. **先に `.htaccess`** — デプロイで新しい内容に置き換わっています
   （置き換わっていなければ、隠しファイルの転送設定を確認してください）
2. **次に `index.php`** を削除する
3. **サーバーパネルでキャッシュを削除する**

> **順番が逆だと一時的にサイトが壊れます。**
> `index.php` を先に消すと、WordPressの書き換え規則だけが残った状態になり、
> 存在しないURLへのアクセスがすべてエラーになります。

これでトップページが新サイトに切り替わります。
表示が変わらない場合は、まずキャッシュ削除を試してください。

**`.user.ini` は残してください。** XserverのPHP設定（アップロード上限など）で、
削除するとPHPの挙動が変わる可能性があります。

### ステップ6：旧ファイルの削除（数日〜1週間おいてから）

問題が出ないことを確認してから、以下を削除します。

```
wp/        ← WordPress本体。フォームが新しい方で動いていることを確認してから
css/
images/
js/
scss/
```

**急ぐ必要はありません。** ただし `wp/` を残したままにするなら、
**WordPressの更新は継続してください。** 放置されたWordPressは改ざん被害の定番の入口です。
使わないなら消すのが最も安全です。

### ステップ7：計測の立ち上げ

検索流入がほぼない状態から始めるので、**ここが今回いちばん効きます。**

- Google Search Console にプロパティを登録し、`sitemap.xml` を送信
- GA4 を全ページに設置
- `thanks.html` / `dl-thanks.html` をコンバージョンとして設定
- GA4を入れたらプライバシーポリシー10章に追記（[公開前チェックリスト.md](公開前チェックリスト.md) B-4）

---

## 最小限の301リダイレクト（必要な場合のみ）

名刺・パンフレット等に旧URLを記載している場合だけ、`.htaccess` に数行足します。
ルートの `.htaccess` にコメントアウトした雛形が入っています。

```apache
RewriteRule ^company/?$        /company.html   [R=301,L]
RewriteRule ^contact/?$        /contact.html   [R=301,L]
```

該当がなければ何もしなくて構いません。旧URLは404ページ（TOPと問い合わせへの導線あり）が出ます。

---

## ニュース更新をどうするか

| 更新頻度 | おすすめ |
|---|---|
| 月1回以下 | **静的のまま**。`news.html` を公開設定に変えてHTMLを直接編集 |
| 月数回以上 | **`wp/` を残してブログだけWordPress**（更新の継続が前提） |
| 管理画面は欲しいが保守は嫌 | **microCMS等のヘッドレスCMS**。静的のまま更新画面を持てる |

---

## 切り替え後のチェック

- [ ] トップページが新サイトになっている
- [ ] 全ページが表示され、リンク切れがない
- [ ] お問い合わせフォームから送信でき、`contact@oym.co.jp` に届く
- [ ] 自動返信が届く
- [ ] 資料ダウンロードが動作し、PDFの直リンクが403になる
- [ ] 存在しないURLで404ページが出て、**HTTPステータスも404**である
- [ ] https で表示され、http と www が正しくリダイレクトされる
- [ ] `https://oym.co.jp/wp/wp-admin/` が不要なら塞がれている
- [ ] Search Console に登録し、`sitemap.xml` を送信した
- [ ] GA4 を設置し、thanks / dl-thanks をコンバージョン設定した
- [ ] バックアップ（ファイル＋DB）を**サーバー外**に保管した
