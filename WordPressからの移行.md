# 現行WordPress → 新サイト（静的HTML）への全面切り替え

**方針：現行サイトは全面的に作り替える。検索流入がほぼないため、
旧URLの引き継ぎ（301リダイレクト）は最小限でよい。**

`/oym.co.jp/public_html/` で現行WordPressが稼働している前提の手順です。

---

## 静的HTMLで問題ありません

新サイトがWordPressに適していないのではなく、**このサイトにWordPressが過剰**です。

| | WordPress | 静的HTML（新サイト） |
|---|---|---|
| 表示速度 | PHP+DB処理が毎回走る | ファイルを返すだけ。圧倒的に速い |
| 保守 | 本体・プラグイン・PHPの更新が継続的に必要 | 不要 |
| セキュリティ | 更新を止めると改ざんの標的になる | 攻撃対象がほぼない |
| 月額コスト | サーバー代＋保守 | サーバー代のみ |

**お問い合わせフォームはXserverのPHPで動くので、WordPressは不要です。**

判断が変わるのは記事を月に何度も更新したい場合だけです（→「ニュース更新をどうするか」）。

---

## ⚠ 消す前に、これだけは確認してください

「検索流入がない」＝「中身が要らない」ではありません。**削除は元に戻せません。**

- [ ] **ブログ記事・お知らせ**に、新サイトへ持っていきたい文章がないか
- [ ] **実績紹介・導入事例・採用情報**など、新サイトに無いページがないか
- [ ] **問い合わせの過去ログ**（Contact Form 7のDB保存プラグインを使っている場合、
      記事ではなくデータベースの中にあります）
- [ ] 現行サイトのURLを**名刺・パンフレット・求人媒体・SNSプロフィール**に
      印刷／掲載していないか（検索流入がなくても、ここからの直接アクセスはあります）

最後の項目だけは要注意です。たとえば名刺に `https://oym.co.jp/company/` と印刷していれば、
そのURLは生かす必要があります。→ 下の「最小限の301リダイレクト」へ。

---

## 手順

### 1. `new/` で先行確認（現行サイトには一切触れません）

GitHubの Variables で `DEPLOY_DIR` を **`/oym.co.jp/public_html/new/`** にしてデプロイ。
`https://oym.co.jp/new/` で全ページの表示とフォーム送信を確認します。

> この段階では新サイトのフォームはまだ動きません。
> `form/config.php` を手動で設置してから確認してください（[フォーム設置手順.md](フォーム設置手順.md)）。

### 2. バックアップを取得

サーバーパネル →「バックアップ」

**★ WordPressは「ファイル」と「データベース（MySQL）」の両方が必要です。**
ファイルだけ取ってもブログ記事は復元できません。記事はDBの中にあります。

ダウンロードしたバックアップは**サーバー外（社内PC・クラウド）に保管**してください。
サーバー上に置いたままでは、サーバーの事故で一緒に失われます。

### 3. 現行ファイルを削除

FTPソフトかサーバーパネルの「ファイル管理」で、`public_html/` の中身を削除します。

**★ 特に次の2つは必ず消してください。残っていると新サイトの表示がおかしくなります。**

| ファイル | 残すとどうなるか |
|---|---|
| `index.php` | トップページがWordPress側に取られる可能性がある |
| `.htaccess`（WordPressのもの） | リクエストがWordPressへ回され、ページが表示されない |

その他 `wp-admin/` `wp-includes/` `wp-content/` `wp-config.php` も削除します。
`wp-config.php` にはデータベースの接続情報が入っているので、**残すほうが危険**です。

> 自動デプロイでは `wp-*` を除外しているため、これらは自動では消えません。
> 事故で消さないための保険であって、共存させる設計ではありません。手動で削除してください。

### 4. 本番へデプロイ

`DEPLOY_DIR` を **`/oym.co.jp/public_html/`** に変更して実行。

### 5. `.htaccess` と `form/config.php` を設置

どちらも自動デプロイの対象外です（[自動デプロイ設定.md](自動デプロイ設定.md) 参照）。

- `.htaccess.sample` の内容を `.htaccess` としてルートに設置
- `form/config.sample.php` を参考に `form/config.php` を作成

### 6. 計測をゼロから立ち上げる

検索流入がほぼない状態から始めるので、**ここが今回いちばん効きます。**

- Google Search Console にプロパティを登録し、`sitemap.xml` を送信
- GA4 を全ページに設置
- `thanks.html` / `dl-thanks.html` をコンバージョンとして設定
- GA4を入れたらプライバシーポリシー10章に追記（[公開前チェックリスト.md](公開前チェックリスト.md) B-4）

---

## 最小限の301リダイレクト（必要な場合のみ）

名刺・パンフレット等に旧URLを記載している場合だけ、`.htaccess` に数行足します。

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On

  # 旧URL → 新URL（301＝恒久的な移転）
  RewriteRule ^company/?$        /company.html          [R=301,L]
  RewriteRule ^contact/?$        /contact.html          [R=301,L]
  RewriteRule ^privacy-policy/?$ /privacy.html          [R=301,L]
</IfModule>
```

`.htaccess.sample` にコメントアウトした雛形を入れてあります。
該当する旧URLが分かれば、行を足すだけです。

**どのURLを残すべきか分からない場合**は、Search Console の「ページ」レポート、
またはブラウザで `https://oym.co.jp/wp-sitemap.xml` を開けば現行URLの一覧が出ます。
一覧を共有いただければリダイレクト設定を作成します。

**該当がなければ何もしなくて構いません。** 旧URLは404ページ（TOPと問い合わせへの導線あり）が出ます。

---

## ニュース更新をどうするか

| 更新頻度 | おすすめ |
|---|---|
| 月1回以下 | **静的のまま**。`news.html` を公開設定に変えてHTMLを直接編集 |
| 月数回以上 | **`/blog/` だけWordPressを残す**。トップと会社案内は静的、ブログのみWP |
| 管理画面は欲しいが保守は嫌 | **microCMS等のヘッドレスCMS**。静的のまま更新画面を持てる |

`/blog/` を残す場合、デプロイは `public_html/` 直下に転送するので、
`public_html/blog/` のWordPressは共存できます。ただし**そのWordPressの更新は継続してください。**
放置されたWordPressは、規模を問わず改ざん被害の定番の入口です。

---

## 切り替え後のチェック

- [ ] トップページが新サイトになっている（`index.php` の残骸に注意）
- [ ] 全ページが表示され、リンク切れがない
- [ ] `https://oym.co.jp/wp-admin/` にアクセスできなくなっている
- [ ] お問い合わせフォームから送信でき、`contact@oym.co.jp` に届く
- [ ] 資料ダウンロードが動作し、PDFの直リンクが403になる
- [ ] 存在しないURLで404ページが出て、**HTTPステータスも404**である
- [ ] https で表示され、http と www が正しくリダイレクトされる
- [ ] Search Console に登録し、`sitemap.xml` を送信した
- [ ] GA4 を設置し、thanks / dl-thanks をコンバージョン設定した
- [ ] バックアップを**サーバー外**に保管した
